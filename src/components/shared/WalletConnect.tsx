import { ConnectButton } from "@rainbow-me/rainbowkit";

const WalletConnect = () => {
   return (
      <div className="wallet-connect-wrapper uppercase">
         <ConnectButton />
      </div>
   );
};

export default WalletConnect;
